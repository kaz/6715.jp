name: deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive
      - run: ./tasks.sh build
      - run: ./tasks.sh deploy
        env:
          GITHUB_TOKEN: ${{ github.token }}
