on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sudo -E make deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
